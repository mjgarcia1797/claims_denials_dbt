version: 2

sources:
  - name: raw
    description: "Raw inputs for the project. In this repo they are seeded but represent upstream ingestion tables."
    schema: main
    tables:
      - name: claim_lines
        description: "Claim line records (raw input)."

      - name: denials
        description: "Denial events at the claim line level."

      - name: appeals
        description: "Appeal records tied to denials."